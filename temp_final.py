classifier.show_informative_features(5)

# --------------------------------------------------------------------
# appending the dataset with new records

    # csvRow = ''.join(row)
    # print(csvRow)
    # fields = [csvRow, prob]
    # with open(r'dataset.csv', 'a') as f:
    #    writer = csv.writer(f)
    #    writer.writerow(fields)
